---
phase: 05-seo-aeo-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - lib/site-config.ts
autonomous: true

must_haves:
  truths:
    - "Every page inherits proper metadataBase for absolute URLs"
    - "Open Graph and Twitter cards render correctly when shared"
    - "Root layout exports comprehensive default metadata"
  artifacts:
    - path: "lib/site-config.ts"
      provides: "Centralized site configuration"
      exports: ["siteConfig"]
    - path: "app/layout.tsx"
      provides: "Root metadata with OG tags"
      contains: "metadataBase"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/site-config.ts"
      via: "import siteConfig"
      pattern: "siteConfig\\."
---

<objective>
Install SEO/AEO packages and configure comprehensive root metadata with Open Graph tags.

Purpose: Establish the metadata foundation that all pages will inherit from, ensuring proper absolute URLs for social sharing and SEO.
Output: Working root layout with metadataBase, Open Graph, Twitter cards, and default SEO metadata.
</objective>

<execution_context>
@C:\Users\jonge\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jonge\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-aeo-performance/05-RESEARCH.md
@app/layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install SEO and Analytics packages</name>
  <files>package.json</files>
  <action>
Install required packages for Phase 5:

```bash
bun add schema-dts @next/third-parties @vercel/analytics next-aeo
```

Packages:
- schema-dts: Type-safe JSON-LD schema markup (Google-maintained)
- @next/third-parties: Optimized Google Analytics 4 integration
- @vercel/analytics: Web Vitals tracking for Vercel deployments
- next-aeo: Automatic llms.txt generation for AI assistants

Do NOT install @types/node (already in devDependencies).
  </action>
  <verify>
`bun install` succeeds and packages appear in package.json dependencies.
Check: `grep "schema-dts" package.json` returns a match.
  </verify>
  <done>All 4 packages installed and listed in package.json dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create site configuration file</name>
  <files>lib/site-config.ts</files>
  <action>
Create a centralized configuration file for site-wide constants used across SEO, metadata, and schema markup.

```typescript
// lib/site-config.ts
export const siteConfig = {
  name: "Jon Gerton Web Design",
  shortName: "Jon Gerton",
  description: "Professional WordPress websites starting at $500. Custom web applications and team growth consulting.",
  url: process.env.NEXT_PUBLIC_SITE_URL || "https://jpgerton.com",
  author: {
    name: "Jon Gerton",
    email: "jon@jpgerton.com",
    jobTitle: "Web Designer & Developer",
    linkedin: "https://linkedin.com/in/jongerton",
    github: "https://github.com/jgerton",
  },
  keywords: [
    "web design",
    "WordPress",
    "freelance developer",
    "web development",
    "portfolio",
    "local business websites",
    "$500 websites",
  ],
  locale: "en_US",
  themeColor: "#000000",
} as const;
```

This provides a single source of truth for all metadata and schema markup.
  </action>
  <verify>
File exists at lib/site-config.ts and exports siteConfig.
TypeScript compiles without errors: `bun run type-check`
  </verify>
  <done>Site configuration file created with all required constants.</done>
</task>

<task type="auto">
  <name>Task 3: Update root layout with comprehensive metadata</name>
  <files>app/layout.tsx</files>
  <action>
Update app/layout.tsx to include full SEO metadata configuration:

1. Import siteConfig from lib/site-config.ts
2. Add metadataBase with proper URL
3. Add title template pattern for child pages
4. Add comprehensive Open Graph tags with default image
5. Add Twitter card configuration
6. Add robots configuration (index only in production)
7. Add icons configuration for favicon and apple-touch-icon
8. Keep existing Inter font setup and Providers wrapper

The metadata export should include:
- metadataBase: new URL(siteConfig.url)
- title: { template: "%s | Jon Gerton", default: siteConfig.name }
- description: siteConfig.description
- keywords: siteConfig.keywords
- authors: [{ name: siteConfig.author.name, url: siteConfig.url }]
- creator: siteConfig.author.name
- openGraph: { type, locale, url, siteName, title, description, images }
- twitter: { card: "summary_large_image", title, description, creator, images }
- robots: { index: process.env.NODE_ENV === "production", follow: true }
- icons: { icon: "/favicon.ico", apple: "/apple-touch-icon.png" }

Note: The og-image.jpg file will be created later (placeholder reference is fine).

Do NOT add analytics components yet - that's Plan 05.
  </action>
  <verify>
- `bun run type-check` passes
- `bun run build` succeeds
- View source of built page shows proper meta tags
  </verify>
  <done>
Root layout exports comprehensive metadata with metadataBase, Open Graph, Twitter cards, and SEO defaults.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `bun run type-check` passes
2. `bun run build` succeeds
3. Inspect build output HTML - should see Open Graph meta tags
4. Check that all pages inherit title template from root
</verification>

<success_criteria>
- [ ] schema-dts, @next/third-parties, @vercel/analytics, next-aeo in package.json
- [ ] lib/site-config.ts exports siteConfig with author, name, url, keywords
- [ ] app/layout.tsx has metadataBase pointing to site URL
- [ ] app/layout.tsx has Open Graph tags with default image
- [ ] app/layout.tsx has Twitter card configuration
- [ ] Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-aeo-performance/05-01-SUMMARY.md`
</output>
