---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .gitignore
  - .eslintrc.json
  - .prettierrc.json
autonomous: true

must_haves:
  truths:
    - "Git repository has develop as default working branch"
    - "Main branch exists for production releases"
    - "Next.js app compiles without TypeScript errors"
    - "ESLint runs without blocking errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "app/layout.tsx"
      provides: "Root layout with Inter font"
      contains: "RootLayout"
    - path: "app/page.tsx"
      provides: "Home page placeholder"
      contains: "export default"
    - path: ".eslintrc.json"
      provides: "ESLint configuration"
      contains: "next/core-web-vitals"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Initialize Next.js 14 App Router project with Git branching strategy.

Purpose: Establishes the foundation that all other infrastructure builds upon - the codebase itself with proper version control.
Output: Working Next.js skeleton with develop/main branching, TypeScript, ESLint, and Prettier configured.
</objective>

<execution_context>
@C:\Users\jonge\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jonge\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with Bun</name>
  <files>
    package.json
    tsconfig.json
    next.config.js
    app/layout.tsx
    app/page.tsx
    app/globals.css
    .gitignore
    tailwind.config.ts
    postcss.config.mjs
  </files>
  <action>
    Run `bunx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` in project root.

    This creates the Next.js 14 App Router skeleton with:
    - TypeScript configuration
    - Tailwind CSS (will be customized in Plan 04)
    - ESLint with next/core-web-vitals
    - App Router structure (app/ directory)
    - Import alias @/* for clean imports

    Accept defaults for all prompts. The command runs in current directory (.) since project already exists.

    After creation, update app/layout.tsx to use Inter font with next/font/google:
    - Import Inter from "next/font/google"
    - Configure with subsets: ["latin"], display: "swap", variable: "--font-inter"
    - Apply inter.variable to html className
    - Apply font-sans to body className

    Update app/page.tsx to a minimal placeholder:
    - Simple h1 with "jpgerton.com" text
    - Centered layout with Tailwind classes
    - Remove default Next.js boilerplate
  </action>
  <verify>
    `bun run build` completes successfully
    `bun run lint` shows no errors
  </verify>
  <done>
    Next.js project exists with TypeScript, App Router structure, and Inter font configured in root layout
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint and Prettier</name>
  <files>
    .eslintrc.json
    .prettierrc.json
    package.json
  </files>
  <action>
    Install dev dependencies:
    `bun add -d @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-config-prettier prettier`

    Update .eslintrc.json with extended configuration:
    ```json
    {
      "extends": [
        "next/core-web-vitals",
        "eslint:recommended",
        "plugin:@typescript-eslint/recommended",
        "prettier"
      ],
      "parser": "@typescript-eslint/parser",
      "plugins": ["@typescript-eslint"],
      "rules": {
        "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }],
        "@typescript-eslint/no-explicit-any": "warn"
      }
    }
    ```

    Create .prettierrc.json:
    ```json
    {
      "semi": true,
      "trailingComma": "es5",
      "singleQuote": false,
      "printWidth": 80,
      "tabWidth": 2,
      "useTabs": false
    }
    ```

    Add scripts to package.json:
    - "format": "prettier --write ."
    - "type-check": "tsc --noEmit"

    Run `bun run format` to apply consistent formatting.
  </action>
  <verify>
    `bun run lint` passes
    `bun run type-check` passes
    `bun run format` runs without errors
  </verify>
  <done>
    ESLint configured with TypeScript support, Prettier configured, no conflicts between them (prettier extends last)
  </done>
</task>

<task type="auto">
  <name>Task 3: Setup Git branching strategy</name>
  <files>
    .gitignore
  </files>
  <action>
    Current state: On master branch with planning docs.

    1. Update .gitignore to ensure it includes:
       - node_modules
       - .next
       - .env*.local (but keep .env.example)
       - *.log
       - .DS_Store
       - .vercel
       - coverage

    2. Stage and commit all new files:
       `git add -A`
       `git commit -m "feat(infra): initialize Next.js 14 with TypeScript and ESLint"`

    3. Rename master to main:
       `git branch -m master main`

    4. Create develop branch from main:
       `git checkout -b develop`

    5. Verify branch structure:
       - main: production releases
       - develop: default working branch

    Note: GitHub default branch setting and protection rules will be configured in Plan 05 after GitHub remote is connected.
  </action>
  <verify>
    `git branch` shows both main and develop
    `git log --oneline -3` shows commit history
    Currently on develop branch
  </verify>
  <done>
    Git has main and develop branches, currently on develop, all project files committed
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `bun run build` succeeds
2. `bun run lint` shows no errors
3. `bun run type-check` passes
4. `git branch` shows main and develop
5. Project structure exists: app/layout.tsx, app/page.tsx, package.json
</verification>

<success_criteria>
- Next.js 14 App Router project compiles and lints cleanly
- Git repo has main (production) and develop (working) branches
- ESLint + Prettier configured without conflicts
- Inter font configured in root layout
- Ready for Docker containerization in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
