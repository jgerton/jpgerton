---
phase: 10-typography-color-system
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - components/portfolio/hero-section.tsx
  - app/(home)/page.tsx
  - app/about/page.tsx
  - app/services/page.tsx
  - app/contact/page.tsx
  - app/projects/page.tsx
  - app/projects/[slug]/page.tsx
  - app/contact/thank-you/page.tsx
autonomous: true

must_haves:
  truths:
    - "H1, H2, H3 headings on all public pages render in Lora (serif) font"
    - "H4-H6 headings, body text, and UI elements render in Inter (sans-serif)"
    - "Heading sizes use fluid type tokens (text-h1, text-h2, etc.) instead of fixed Tailwind sizes"
    - "Body text paragraphs maintain 50-75 characters per line on desktop"
    - "Line heights are set appropriately: tight for large headings, normal/relaxed for body"
    - "Hero section uses hero-sized text token with Lora italic for the tagline"
  artifacts:
    - path: "components/portfolio/hero-section.tsx"
      provides: "Hero with font-serif H1, italic tagline, fluid hero sizing"
      contains: "font-serif"
    - path: "app/about/page.tsx"
      provides: "About page with serif H1/H2/H3, line length constraints, fluid sizing"
      contains: "font-serif"
    - path: "app/services/page.tsx"
      provides: "Services page with serif headings and fluid sizing"
      contains: "font-serif"
  key_links:
    - from: "all public pages"
      to: "app/globals.css @theme tokens"
      via: "Tailwind utilities: font-serif, text-h1, text-hero, leading-tight, max-w-[65ch]"
      pattern: "font-serif.*text-h[1-3]"
    - from: "components/portfolio/hero-section.tsx"
      to: "app/globals.css"
      via: "text-hero fluid token for 60-72px H1"
      pattern: "text-hero"
---

<objective>
Apply the Lora serif font, fluid type scale tokens, line height standards, and readable line length constraints across all public-facing pages and key components.

Purpose: This transforms the site from generic Inter-only typography to a warm, professional Lora + Inter pairing. Every public page gets serif headings (H1-H3), fluid sizing, proper line heights, and readable line lengths. The hero section gets special treatment with hero-sized Lora and an italic tagline.

Output: Updated public pages and hero component with consistent typography system applied.
</objective>

<execution_context>
@C:\Users\jonge\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jonge\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-typography-color-system/10-CONTEXT.md
@.planning/phases/10-typography-color-system/10-RESEARCH.md
@.planning/phases/10-typography-color-system/10-01-SUMMARY.md
@app/globals.css
@components/portfolio/hero-section.tsx
@app/(home)/page.tsx
@app/about/page.tsx
@app/services/page.tsx
@app/contact/page.tsx
@app/projects/page.tsx
@app/projects/[slug]/page.tsx
@app/contact/thank-you/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply serif font and fluid type tokens to hero section and home page</name>
  <files>components/portfolio/hero-section.tsx, app/(home)/page.tsx</files>
  <action>
    Update `components/portfolio/hero-section.tsx`:
    - H1: Change from `text-4xl md:text-5xl font-bold` to `font-serif font-semibold text-hero leading-tight`
      - font-serif = Lora, font-semibold = 600 weight (SemiBold for primary H1 emphasis per CONTEXT.md), text-hero = 60-72px fluid, leading-tight = 1.1
    - Tagline p: Change from `text-lg md:text-xl text-muted-foreground` to `font-serif italic text-h5 leading-snug text-muted-foreground`
      - Lora italic for hero tagline per CONTEXT.md decision. text-h5 for a size that complements the hero H1.
    - Keep `mb-md`, `mb-xl`, `max-w-2xl mx-auto`, and the CTA buttons unchanged.

    Update `app/(home)/page.tsx`:
    - "Recent Projects" H2: Change from `text-3xl font-bold` to `font-serif font-medium text-h2 leading-tight`
      - font-serif = Lora, font-medium = 500 weight per CONTEXT.md, text-h2 = fluid size, leading-tight = 1.1
    - Keep all other classes unchanged (mb-xs, text-center, etc.)

    Typography rules from CONTEXT.md (apply consistently in all tasks):
    - H1, H2, H3: `font-serif` (Lora). H1 uses font-semibold (600). H2, H3 use font-medium (500).
    - H4, H5, H6: `font-sans` (Inter, already default). Keep font-semibold or font-medium as appropriate.
    - Hero H1 only: `text-hero` (60-72px). All other H1s: `text-h1`.
    - Line heights: H1/H2 get `leading-tight` (1.1). H3/H4 get `leading-snug` (1.25). Body gets default (1.5) or `leading-relaxed` (1.6) for long-form.
    - Do NOT change spacing tokens (mb-xs, mb-md, etc.), layout classes, or component structure.
  </action>
  <verify>
    Run `bun run build` to confirm no errors. Visually inspect home page in browser: hero H1 should render in Lora serif at large fluid size, tagline in Lora italic, "Recent Projects" in Lora serif.
  </verify>
  <done>
    Hero section renders H1 in Lora SemiBold at 60-72px fluid size with 1.1 line height. Tagline renders in Lora italic. Home page "Recent Projects" H2 renders in Lora Medium with fluid sizing. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply typography system to all remaining public pages with line length constraints</name>
  <files>app/about/page.tsx, app/services/page.tsx, app/contact/page.tsx, app/projects/page.tsx, app/projects/[slug]/page.tsx, app/contact/thank-you/page.tsx</files>
  <action>
    Apply consistent typography across all remaining public pages. For each page:

    **Heading updates (apply font-serif + fluid tokens):**
    - All H1 elements: Add `font-serif font-semibold text-h1 leading-tight`. Remove old `text-4xl font-bold tracking-tight sm:text-5xl` patterns.
    - All H2 elements: Add `font-serif font-medium text-h2 leading-tight`. Remove old `text-2xl font-semibold` or `text-3xl font-bold`.
    - All H3 elements: Add `font-serif font-medium text-h3 leading-snug`. Remove old `font-semibold` or `font-medium` size patterns. Note: H3 in cards (e.g., "No Jargon", "Fixed Pricing") may remain as `font-sans` if they are more UI-label than section heading. Use judgment: if it reads as a section heading, make it serif. If it's a card label/title inside a card component, keep it sans.
    - H4 and smaller: Keep `font-sans` (Inter). Update to fluid tokens if appropriate (text-h4, text-h5, text-h6).
    - Stat numbers (like "27+", "5 Days"): Keep as font-sans font-bold. These are data displays, not headings. Update from `text-3xl` to `text-h3` for fluid sizing.

    **Line height updates:**
    - H1, H2: `leading-tight`
    - H3, H4: `leading-snug`
    - Body paragraphs in long-form sections (about page prose): `leading-relaxed` (1.6)
    - Short paragraphs (descriptions, card content): default line height (no explicit leading class needed, body default is 1.5)

    **Readable line length (50-75 characters):**
    - About page: The main prose content area already has `prose prose-lg dark:prose-invert max-w-none`. Change `max-w-none` to `max-w-prose` (which is ~65ch in Tailwind). This constrains body text paragraphs to readable width while keeping the overall page layout intact.
    - Services page FAQ answers: Already constrained by `max-w-3xl mx-auto`. This is approximately 65ch. Keep as-is.
    - Contact page: Body text already within card layouts (naturally constrained). No changes needed.
    - Projects page: Description text is short. No line length constraint needed.
    - For subtitle/description paragraphs under page headings (e.g., "max-w-2xl mx-auto"): These are already constrained. Keep as-is.

    **Specific page notes:**

    `app/about/page.tsx`:
    - Hero H1 "Hey, I'm Jon": `font-serif font-semibold text-h1 leading-tight`. Remove `text-4xl font-bold tracking-tight sm:text-5xl`.
    - Section H2s ("Background", "My Approach", etc.): `font-serif font-medium text-h2 leading-tight`. Remove `text-2xl font-semibold`.
    - "Ready to Get Started?" H2: Same serif treatment.
    - Card H3s ("No Jargon", "Fixed Pricing", etc.): Keep as `font-sans font-semibold` (these are card labels, not section headings). No font-serif.
    - Prose section: Change `max-w-none` to `max-w-prose` for readable line length.
    - Subtitle paragraph: Add `leading-relaxed` to the description text.

    `app/services/page.tsx`:
    - H1 "Services": `font-serif font-semibold text-h1 leading-tight`. Remove old sizing.
    - H2 "Frequently Asked Questions": `font-serif font-medium text-h2 leading-tight`.
    - H2 "Not sure which option is right for you?": Same.
    - FAQ H3 questions: Keep as `font-sans font-medium` (UI text inside bordered cards).
    - Stat numbers: Keep font-sans, update to `text-h3` for fluid sizing.

    `app/contact/page.tsx`:
    - H1 "Let's Build Something Together": `font-serif font-semibold text-h1 leading-tight`.
    - Card titles (CardTitle components): Leave unchanged (these are UI component text).
    - H4 labels ("Email", "GitHub", "LinkedIn"): Keep as font-sans.

    `app/projects/page.tsx`:
    - H1 "All Projects": `font-serif font-semibold text-h1 leading-tight`. Remove `text-4xl font-bold`. Apply to BOTH the content and Suspense fallback versions.

    `app/projects/[slug]/page.tsx`:
    - H1 project name: `font-serif font-semibold text-h1 leading-tight`. Remove `text-4xl font-bold`.
    - H2 "Tech Stack": `font-serif font-medium text-h2 leading-tight`.

    `app/contact/thank-you/page.tsx`:
    - H1 heading: `font-serif font-semibold text-h1 leading-tight`. Remove old sizing.

    IMPORTANT: Do NOT change admin pages (app/admin/*). Admin pages use Inter only and are out of scope for this phase.
  </action>
  <verify>
    Run `bun run build` to confirm no errors. Check each public page in the browser:
    1. Home: Already done in Task 1
    2. About: H1 and H2s in Lora, body text has readable line length
    3. Services: H1 and H2s in Lora, FAQ questions in Inter
    4. Contact: H1 in Lora, card titles in Inter
    5. Projects: H1 in Lora
    6. Project detail: H1 and H2 in Lora
    7. Thank you: H1 in Lora
  </verify>
  <done>
    All public page headings (H1, H2, H3 where appropriate) render in Lora serif with fluid type tokens. Body text uses Inter. Line heights are set: tight for H1/H2, snug for H3/H4, relaxed for long-form prose. About page prose content has max-w-prose for readable line length (approximately 65 characters). Build passes with no errors.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completes without errors
2. All public page H1 headings render in Lora serif font
3. All public page H2 headings render in Lora serif font
4. Body text and UI elements render in Inter sans-serif
5. Hero H1 renders at large fluid size (60-72px desktop range)
6. Typography scales fluidly when resizing browser window (no breakpoint jumps)
7. About page body text maintains approximately 50-75 characters per line
8. Admin pages are unaffected (still use Inter throughout)
</verification>

<success_criteria>
- Every public page H1/H2 uses font-serif (Lora) with appropriate fluid type token
- Hero section uses text-hero (60-72px), hero tagline uses Lora italic
- Fluid sizing scales smoothly from mobile to desktop on all headings
- Line heights follow the inverse scaling pattern (tight for large, relaxed for body)
- Body text on the about page has readable line length (~65 characters per line)
- No admin page changes
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-typography-color-system/10-02-SUMMARY.md`
</output>
